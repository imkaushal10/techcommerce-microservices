apiVersion: v1
kind: Service
metadata:
  name: order-api-service
  namespace: techcommerce
  labels:
    app: order-api
spec:
  type: ClusterIP
  selector:
    app: order-api
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
